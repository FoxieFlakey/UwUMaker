[Project]
name = "Sample"
type = "executable"

# If CONFIG_* is y then for non array
# its overwritten
name-CONFIG_SWAP_NAME = "Swapped"

# Subdir section
[Subdir]
c-sources = [ "main.c" ]

# If CONFIG_* is y then for arrays
# its appended
c-sources-CONFIG_COMPILE_EXTRA = [ "common.c" ]

